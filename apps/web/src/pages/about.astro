---
import Layout from '../layouts/Layout.astro';
import { css } from '../../styled-system/css';
import { stack, hstack, grid } from '../../styled-system/patterns';
import { Image } from 'astro:assets';
import profileImage from '../assets/images/profile.svg';
import githubLogo from '../assets/images/github-logo.svg';
import twitterLogo from '../assets/images/twitter-logo.svg';

const techStack = [
  { name: 'Astro', description: 'The web framework' },
  { name: 'SolidJS', description: 'UI Library' },
  { name: 'Panda CSS', description: 'Styling engine' },
  { name: 'Cloudflare', description: 'Deployment & R2' },
  { name: 'Neovim', description: 'Editor of choice' }
];

const socialLinks = [
  { name: 'GitHub', url: 'https://github.com/koralle', icon: githubLogo },
  { name: 'Twitter', url: 'https://twitter.com', icon: twitterLogo }
];
---

<Layout title="About Me" description="About koralle and this blog">
  <div class={stack({ gap: '16', py: '12', maxWidth: '600px', mx: 'auto' })}>
    {/* Profile Header */}
    <header class={stack({ gap: '6', alignItems: 'center', textAlign: 'center' })}>
      <div
        class={css({
          width: '32',
          height: '32',
          rounded: 'full',
          overflow: 'hidden',
          bg: 'gray.100',
          borderWidth: '2px',
          borderColor: 'gray.200'
        })}
      >
        <Image src={profileImage} alt="koralle" class={css({ width: '100%', height: '100%', objectFit: 'cover' })} />
      </div>
      <div class={stack({ gap: '2' })}>
        <h1 class={css({ fontSize: '3xl', fontWeight: 'bold', color: 'gray.900' })}>koralle</h1>
        <p class={css({ fontSize: 'lg', color: 'gray.600' })}>Software Engineer / Vim Enthusiast</p>
      </div>

      {/* Social Links */}
      <div class={hstack({ gap: '4' })}>
        {
          socialLinks.map((link) => (
            <a
              href={link.url}
              target="_blank"
              rel="noopener noreferrer"
              class={css({
                display: 'flex',
                alignItems: 'center',
                gap: '2',
                padding: '2',
                rounded: 'md',
                color: 'gray.600',
                transition: 'all 0.2s',
                hover: { color: 'gray.900', bg: 'gray.100' }
              })}
            >
              <Image src={link.icon} alt={link.name} width={20} height={20} />
              <span class={css({ fontSize: 'sm', fontWeight: 'medium' })}>{link.name}</span>
            </a>
          ))
        }
      </div>
    </header>

    {/* Bio Section */}
    <section class={stack({ gap: '4' })}>
      <h2
        class={css({
          fontSize: 'xl',
          fontWeight: 'bold',
          color: 'gray.800',
          borderBottomWidth: '1px',
          borderBottomColor: 'gray.200',
          pb: '2'
        })}
      >
        About Me
      </h2>
      <div class={stack({ gap: '4', color: 'gray.700', lineHeight: 'relaxed' })}>
        <p>
          日々の開発で得た知見や、気になった技術について書き留める場所です。
          技術的な正確さを追求しつつ、読み物としても楽しめるコンテンツを目指しています。
        </p>
        <p>
          主な興味領域はフロントエンド開発、UI/UXデザイン、そして開発者体験（DX）の向上です。
          特に Vim/Neovim を中心とした効率的な開発フローの構築に情熱を注いでいます。
        </p>
      </div>
    </section>

    {/* Tech Stack Section */}
    <section class={stack({ gap: '4' })}>
      <h2
        class={css({
          fontSize: 'xl',
          fontWeight: 'bold',
          color: 'gray.800',
          borderBottomWidth: '1px',
          borderBottomColor: 'gray.200',
          pb: '2'
        })}
      >
        Tech Stack
      </h2>
      <p class={css({ color: 'gray.600', fontSize: 'sm', mb: '2' })}>
        このブログは以下の技術で構築されています。
      </p>
      <div class={grid({ columns: { base: 1, sm: 2 }, gap: '4' })}>
        {
          techStack.map((tech) => (
            <div
              class={stack({
                gap: '1',
                p: '4',
                rounded: 'lg',
                borderWidth: '1px',
                borderColor: 'gray.200',
                bg: 'white',
                transition: 'border-color 0.2s',
                hover: { borderColor: 'rosePink.400' }
              })}
            >
              <span class={css({ fontWeight: 'bold', color: 'gray.800' })}>{tech.name}</span>
              <span class={css({ fontSize: 'sm', color: 'gray.500' })}>{tech.description}</span>
            </div>
          ))
        }
      </div>
    </section>
  </div>
</Layout>
